// height and width of the opened window in pixels
#define WINDOW_WIDTH 800
#define WINDOW_HEIGHT 800

// the side length, in pixels, of each grid pixel
#define PIXEL_SIZE 40

// how much time in between each frame
#define TICK_INTERVAL 10

// how many frames it takes for the snake to advance
#define SNAKE_SPEED 8

// don't edit these
#define HEIGHT WINDOW_HEIGHT / PIXEL_SIZE
#define WIDTH WINDOW_WIDTH / PIXEL_SIZE
#define NUM_PIXELS (HEIGHT * WIDTH)
